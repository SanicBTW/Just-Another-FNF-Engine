package window_ui;

// What if instead of creating a notification tray we just create a new snack bar with some funky arguments???
// Maybe it isn't too good for update and cache but who knows
// Idea tho won't be working on this for now
