package window;

// Class that stores an array of DisplayObjects with a property and a ratio to track
// It will loop for each DisplayObject in the array and lerp the property with the ratio
// Todo btw
